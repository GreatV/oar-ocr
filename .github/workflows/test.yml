name: Run Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Run Tests, Lints, and Format Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          # Install components needed for linting and formatting
          components: rustfmt, clippy

      - name: Cache Cargo dependencies
        # Use a dedicated action for caching Rust projects
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy linter
        # Use `cargo clippy --all-targets --all-features -- -D warnings` for full features
        # Run clippy on all workspace members with warnings-as-errors
        run: |
          cargo clippy --all-targets --features visualization -- -D warnings
          cargo clippy --all-targets -p oar-ocr-vl -- -D warnings

      - name: Run tests in debug mode
        run: cargo test --verbose --all
